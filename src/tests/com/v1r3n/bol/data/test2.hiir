#begin
$name = (1+(3+4)+ (getfactor for $age.) + ( age of $applicants. + salary of $applicants.))
(
statement.
satmt2.
	(statment3.)
$age = (1+2)
)
if($name > $aeg) then (someehting. other thign.).
$agefactor as number
get the $agefactor for the $applicant.

#end